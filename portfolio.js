var typed = new Typed('.multiple-text', {
    strings: ['ForntEndDev', 'BackEndDev', 'DevOps'], // Array of strings to type
    typeSpeed: 50,  // Speed at which characters are typed
    backSpeed: 25,   // Speed at which characters are erased
    backDelay: 1000, // Delay before starting to erase
    startDelay: 500, // Delay before starting typing
    loop: true,      // Loop the animation
    showCursor: true // Show the blinking cursor
  });
  